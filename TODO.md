# 다음 작업

> 작업 재개 지점 정리. 마지막 작업일 2026-09-13.

## 재개하는 법

```bash
cd "C:\Users\suhjo\Desktop\3. 개인 프로젝트\3. 홈페이지 만들기"
npm run dev     # → http://localhost:3000
```

구조와 디자인 규칙은 `README.md`, 품질 기준은 `docs/웹사이트-품질-평가기준.md` 참고.
**내용 수정은 `src/content/clinic.ts` 한 파일만** 보면 됩니다.

---

## 현재 상태

Next.js 16 + Tailwind v4. 린트 0건, 빌드 성공, 8개 라우트 전부 정적 생성.
품질 점수 **106 / 130** (평가 기준 13항목, 상세는 아래 "점수 현황").

| 페이지 | 상태 |
| --- | --- |
| `/` 홈 | 완성. 스크롤 확장 히어로 + 7개 섹션 |
| `/implant` | 완성. 구조도 + 치료 과정 5단계 + 5종 |
| `/ortho` | 완성. 장치 비교 도해 + 2종 + 유지 단계 |

---

## A. 내가(사용자가) 준비해야 넘어갈 수 있는 것

우선순위 순. **1~3번이 점수와 신뢰도에 가장 크게 걸립니다.**

### A-1. 영상 워터마크 제거 🔴

`public/video/hero.mp4` 우상단에 **"DeeVid AI"** 워터마크가 박혀 있습니다.
화면 비율에 따라 노출됩니다(축소 패널·16:9 화면에서 보임).
→ 워터마크 없는 원본으로 교체 후 같은 경로에 덮어쓰기.

### A-2. 사진 3종 🔴

| 필요한 사진 | 쓰이는 곳 | 권장 비율 |
| --- | --- | --- |
| 대표원장 프로필 | 홈 · 의료진 | 3:4 세로 |
| 진료실 | 홈 · 둘러보기 | 4:3 가로 |
| (선택) 못 받은 내부 사진 4장 | 홈 · 둘러보기 | 4:3 가로 |

기존 Google Sites에서 2장만 받았고 나머지 4장은 403으로 막혔습니다.
→ `public/photos/` 에 넣고 해당 `<Placeholder />` 를 `<Image />` 로 교체.

### A-3. 법정 고지 정보 🔴

의료기관 개설자명, 사업자등록번호, 개인정보처리방침.
현재 **아예 없습니다.** 실제 운영 전 필수.

### A-4. 실제 도메인

`src/content/clinic.ts` 의 `siteUrl` 이 `https://topdental.kr` 임시값입니다.
sitemap, 카카오톡·네이버 공유 미리보기에 그대로 쓰입니다.

### A-5. 진료시간 확인

- 일요일·공휴일 → 원본에 명시가 없어 **휴진으로 임의 표기**했습니다. 확인 필요
- 점심시간 → 원본에 없어 표기하지 않았습니다. 있으면 알려주세요

### A-6. 예약 경로 결정

현재 **전화 한 가지뿐**입니다. 온라인 예약 폼이나 카카오 채널을 붙일지 결정.

---

## B. 자료 없이 바로 할 수 있는 작업

### B-1. 영상 faststart 최적화

`moov` 아톰이 파일 끝에 있어 1.9MB를 다 받아야 재생이 시작됩니다.
재인코딩 없이 앞으로 옮기면 즉시 시작:

```bash
ffmpeg -i public/video/hero.mp4 -c copy -movflags +faststart public/video/hero.tmp.mp4
mv public/video/hero.tmp.mp4 public/video/hero.mp4
```

※ 이 PC에 ffmpeg이 없어 실행하지 못했습니다.

### B-2. 지도 embed

홈 · 오시는 길이 아직 플레이스홀더입니다. 네이버 또는 카카오 지도 embed 권장.

### B-3. 애니메이션 (점수 6.0 — 유일하게 손대지 않은 항목)

현재 히어로 진입 연출과 스크롤 확장 외에는 사용자 행동에 반응하는 모션이
거의 없습니다. 모바일 메뉴 열림/닫힘 전환 정도가 후보.

### B-4. "지금 진료 중" 표시

`clinic.ts` 의 `hours` 를 현재 시각과 비교해 헤더에 상태를 띄우면
전환 설계 점수가 올라갑니다.

### B-5. 진료 도해 추가

`src/components/diagrams/` 에 같은 방식으로 그리면 됩니다. 후보:
상악동 거상술 전/후 개념도, 뼈이식 차폐막 구조.

> ⚠️ **다른 병원 사이트의 사진·일러스트는 가져오지 마세요.**
> 병원 홈페이지는 영리 목적이라 저작권 문제가 실제로 발생합니다.
> 직접 그리거나 라이선스를 구매한 스톡만 사용.

---

## C. 이미 결정된 사항 (다시 논의 불필요)

- **히어로 연출** — 배경 영상이 **페이지 바탕색에서** 가운데에서 바깥으로 번집니다.
  페이지 진입 직후 자동으로 한 번 재생되고 **스크롤을 가로채지 않습니다.**
  스크롤 잠금 방식으로 되돌리지 말 것 (`src/components/HeroReveal.tsx`)
- **의료진까지는 흰 바탕** — 회색 면은 자주 묻는 질문 · 진료시간 · 푸터에만
- **푸터에 진료시간·오시는 길을 다시 넣지 말 것** — 바로 위 섹션과 중복됩니다
- **히어로 글자색은 고정** — 아래쪽에 바탕색 그라데이션을 항상 깔아 먹색 글자를
  유지합니다. 글자색을 배경과 함께 건너가게 하면 중간에 대비가 무너지므로
  그 방법으로 되돌리지 말 것
- **히어로 영상 워터마크는 CSS 로 잘라냄** — 위쪽 11%를 잘라 가립니다. 대신 인물의
  정수리가 함께 잘립니다. ffmpeg 로 영상 자체를 자르면 `CROP_TOP` 을 `0` 으로
  되돌릴 수 있습니다 (README 참고)
- **진료 원칙은 카드 그리드** — 여섯 가지를 한눈에 펼쳐 둡니다. 넘기지 않습니다
- **임플란트 · 치아교정 · 병원 둘러보기는 카드 넘기기** — 왼쪽 기둥에 제목·안내와
  항목 이름 목록, 오른쪽에 6:4 가로 카드 **한 장** + 좌우 화살표 (`CardRail`).
  이름을 누르면 그 카드로 넘어가고, 화살표로 넘기면 표시가 따라옵니다.
  상세 페이지 "더 보기" 링크는 이 목록으로 대신했고 헤더 메뉴로 들어갑니다
- **발치 즉시 임플란트 그림 필요** — `references/` 에 그 시술 그림만 없어서
  해당 카드는 글만 나옵니다
- **카드 비율은 지지대로 잡음** — `aspect-ratio` + `min-height: fit-content` 는
  크롬에서 비율이 상한이 되어 긴 글이 잘립니다. `.card-ratio` 방식을 유지할 것
- **`scrollbar-gutter: stable` 을 지우지 말 것** — 히어로 스크롤 잠금이 풀릴 때
  스크롤바가 나타나며 배경 영상이 통째로 다시 배치되는 것을 막습니다
- **카드는 흰 바탕 + 그림자** — 그림자를 쓰는 유일한 자리입니다. 나머지 구분은
  전부 헤어라인과 여백
- **홈은 흰 바탕으로 통일** — 임플란트 섹션의 짙은 파랑 면을 걷어 냈습니다.
  짙은 면은 상세 페이지 맨 아래 CTA 에만 남깁니다
- **상세 페이지는 카드로 감추지 않음** — `/implant`, `/ortho` 는 처음부터 전부
  읽으러 오는 곳이라 그대로 나열합니다
- **임플란트 · 교정 제목은 고정하지 않음** — `lg:sticky` 를 뺐습니다. 스크롤에
  따라 제목이 따라다니지 않도록 한 것이므로 다시 넣지 말 것
- **네이버 지도 Client ID 필요** ⚠️ — 지도 컴포넌트(`NaverMap.tsx`)는 다 만들어
  두었고 키만 넣으면 바로 뜹니다. `.env.example` 의 절차대로 발급받아
  `.env.local` 에 `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` 를 채우세요.
  키가 없는 동안에는 네이버 지도 링크로 물러섭니다.
  `clinic.address` 를 고치면 `clinic.mapUrl` 도 같이 고칠 것
- **git init 보류** — 다음에 진행
- **폰트는 Pretendard** — 외부 오리진 요청 0건 유지를 위해 npm 패키지 방식 사용.
  CDN 방식으로 되돌리지 말 것
- **색상 토큰** — `muted`/`faint` 를 더 밝게 바꾸면 WCAG AA 대비가 깨집니다

---

## 점수 현황 (130점 만점, 현재 106점)

| 항목 | 현재 | 올릴 방법 |
| --- | --- | --- |
| 관점 | 8.0 | — |
| 타이포그래피 | 8.0 | — |
| 색상 | 9.0 | — |
| 위계 | 8.5 | — |
| **이미지** | **6.0** | A-1, A-2 |
| **애니메이션** | **6.0** | B-3 |
| 모바일 | 9.0 | — |
| 디테일 | 9.0 | — |
| 카피 | 9.0 | — |
| 성능 | 9.0 | B-1 |
| 접근성 | 9.0 | — |
| **신뢰 근거** | **7.5** | A-2, A-3, B-2 |
| 전환 설계 | 8.5 | A-6, B-4 |

**A-1 ~ A-3 만 해결하면 약 112점**, B 항목까지 하면 118점 권.
